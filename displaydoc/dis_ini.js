$("document").ready(init);

strSeeCBlog = "The code is been migrating, check it if you want now on my Chinese Blog (Just Ignore the Chinese Words:-))";

function init(){
	$(".seeCBlog").html(strSeeCBlog);
}
